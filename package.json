{
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "commander": "^9.2.0"
  },
  "devDependencies": {
    "@types/node": "^14.18.18",
    "gts": "^3.1.0",
    "typescript": "^4.0.3"
  }
}
